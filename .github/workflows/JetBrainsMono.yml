name: JetBrainsMono-patch

on:
  schedule:
    - cron:  '0 10 * * *'
  
  workflow_dispatch:

jobs:
  get-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Poll & patch
        run: ./patch.sh JetBrains/JetBrainsMono
      - name: Commit & push
        run: ./push.sh 